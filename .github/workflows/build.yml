
# build.yml 修复版 - GitHub Actions 工作流配置
# 这是一个完整的 CI/CD 构建配置文件示例

build_yml_content = """name: Build and Deploy

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  NODE_VERSION: '18.x'
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run linter
        run: npm run lint --if-present
      
      - name: Run tests
        run: npm test --if-present
      
      - name: Build project
        run: npm run build --if-present
      
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        if: always()
      
      - name: Build Docker image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          docker build -t ${{ env.REGISTRY }}/${{ github.repository }}:latest .
      
      - name: Login to Container Registry
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Push Docker image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          docker push ${{ env.REGISTRY }}/${{ github.repository }}:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - name: Deploy to production
        run: echo "Deploying to production..."
      
      - name: Notify deployment
        run: echo "Deployment complete"
"""

# 输出修复要点说明
print("=" * 60)
print("build.yml 修复版 - 关键改进点")
print("=" * 60)
print()

print("✓ 工作流触发条件:")
print("  - push: main 和 develop 分支")
print("  - pull_request: main 分支")
print()

print("✓ 构建任务 (build job):")
print("  - 检出代码")
print("  - 配置 Node.js 环境")
print("  - 安装依赖 (使用 npm ci)")
print("  - 运行代码检查 (lint)")
print("  - 运行测试")
print("  - 构建项目")
print("  - 上传测试覆盖率")
print()

print("✓ Docker 部署 (仅在主分支):")
print("  - 构建 Docker 镜像")
print("  - 登录容器仓库")
print("  - 推送镜像")
print()

print("✓ 部署任务 (deploy job):")
print("  - 依赖 build job 成功完成")
print("  - 仅在主分支推送时执行")
print()

print("=" * 60)
print("build.yml 完整配置内容:")
print("=" * 60)
print(build_yml_content)
