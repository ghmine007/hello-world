
import platform
import sys
import subprocess
import os

# Display system information
print("=" * 60)
print("Hello World - 跨平台交叉编译示例")
print("=" * 60)
print()

# 1. 检测当前平台
current_platform = platform.system()
current_arch = platform.machine()
print(f"当前平台: {current_platform}")
print(f"当前架构: {current_arch}")
print(f"Python版本: {sys.version.split()[0]}")
print()

# 2. 不同平台的Hello World变体
platforms_info = {
    "Windows": "cmd.exe /c 'echo Hello World - Windows'",
    "Linux": "bash -c 'echo Hello World - Linux'",
    "Darwin": "bash -c 'echo Hello World - macOS'"
}

print("=" * 60)
print("不同平台的Hello World实现:")
print("=" * 60)
print()

# 3. Python跨平台Hello World（推荐方式）
hello_variants = {
    "标准方式": "print('Hello World')",
    "Unicode方式": "print('Hello World 🌍 平台: ' + platform.system())",
    "格式化方式": f"print(f'Hello World - {current_platform} ({current_arch})')",
    "多语言方式": "print('Hello World | 你好世界 | こんにちは世界 | Привет мир')"
}

for variant_name, code in hello_variants.items():
    print(f"[{variant_name}]")
    print(f"  代码: {code}")
    try:
        exec(code)
    except Exception as e:
        print(f"  错误: {e}")
    print()

# 4. 跨平台兼容代码示例
print("=" * 60)
print("跨平台兼容代码示例:")
print("=" * 60)
print()

def hello_world_cross_platform():
    """跨平台的Hello World函数"""
    import sys
    import platform
    
    os_name = platform.system()
    print(f"Hello World from {os_name}!")
    print(f"Architecture: {platform.machine()}")
    print(f"Python: {sys.version.split()[0]}")

hello_world_cross_platform()
print()

# 5. 编译目标信息（模拟）
print("=" * 60)
print("可能的交叉编译目标:")
print("=" * 60)
targets = [
    ("x86_64-w64-mingw32", "Windows 64-bit (MinGW)"),
    ("i686-w64-mingw32", "Windows 32-bit (MinGW)"),
    ("x86_64-linux-gnu", "Linux x86_64 (GNU)"),
    ("aarch64-linux-gnu", "Linux ARM64 (GNU)"),
    ("x86_64-apple-darwin", "macOS x86_64 (Intel)"),
    ("aarch64-apple-darwin", "macOS ARM64 (Apple Silicon)"),
]

for target, description in targets:
    print(f"  {target:<25} - {description}")
